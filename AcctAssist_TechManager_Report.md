# AcctAssist 系統技術架構報告
### 面向技術主管版本 — 完整自動化技術鏈結說明

---

## 前言

本報告將說明 AcctAssist 從前端 LINE Bot 接入、後端非同步處理、多模態 AI 辨識，到管理控制台審核的完整技術鏈結。整個架構的設計核心在於三點：**最小化用戶操作摩擦、最大化後端自動化程度、確保財務資料的完整可稽核性**。

---

## 第一階段：LINE 端互動與身份動態綁定機制

整個系統的接入點是 LINE Messaging API Webhook。所有來自用戶端的互動事件，無論是文字訊息、圖片上傳或按鈕操作，都統一經由這個 Webhook 端點進入後端服務。

當一位用戶首次發送訊息時，後端會即時執行**身份識別與紀錄自動建檔**邏輯——系統查詢是否存在對應的用戶記錄，不存在則自動建立，確保每一個 LINE 使用者都有唯一對應的系統身份。

接下來是**動態身份綁定流程**。系統透過對話狀態機來管理 Onboarding 進程：

- 若企業啟用了實名制開關，系統會進入「姓名綁定」的對話狀態，引導用戶輸入真實姓名後自動寫入資料庫，完成身份實名化。
- 完成實名綁定後，系統接著透過 Quick Reply 互動元件，引導用戶完成部門選擇並持久化記錄。

這整個身份綁定流程的設計關鍵在於：**狀態機驅動的多輪對話（Multi-turn Dialogue）**，系統會在每一次互動中讀取用戶的當前狀態，決定下一步應執行哪個分支邏輯，直到初始化完成為止。

---

## 第二階段：圖片上傳的後端儲存機制與 60 秒 Debounce 批次採集

當用戶開始上傳憑證圖片時，後端進入**批次採集模式**。每一張圖片上傳後，系統執行以下操作：

1. **圖片下載與本地儲存**：後端服務透過 LINE Content API 非同步下載原始圖片，以 UUID 命名儲存於本地暫存目錄，確保每張圖片路徑唯一不碰撞。

2. **併發安全的 Buffer 累積**：系統採用資料庫層級的行鎖（Row-level Lock）機制，確保在高併發情境下（例如用戶快速連拍上傳），每一張圖片都能被安全地寫入 Buffer 佇列，不會發生競態條件（Race Condition）。Buffer 中每筆記錄除了圖片路徑外，還附帶 LINE 事件的精確毫秒時間戳與訊息 ID，為後續排序與追蹤提供依據。

3. **60 秒滑動視窗 Debounce 機制**：這是批次採集的核心設計。每當有新圖片進入 Buffer，系統就會重置一個 60 秒的延時觸發計時器。這個「滑動視窗」設計讓用戶可以在自然的節奏下連續上傳多張圖片，只要用戶持續上傳，計時器就持續延後。直到用戶停止操作超過 60 秒，計時器才會觸發後端的自動處理流程。

4. **文字備註非同步累積**：用戶在上傳過程中插入的任何文字說明，系統會靜默地附加至本次批次的備註欄位，不打斷主流程，也不回覆多餘訊息。

此外，系統也支援**手動確認送出**路徑：用戶點擊 Rich Menu 的「確認送出」按鈕後，後端立即取消計時器，清空 Buffer 並提交，同時在 500 毫秒內回應確認訊息，確保用戶操作的即時反饋感。

---

## 第三階段：背景任務調用 Gemini OCR 與生成式 AI 語義分析

無論是手動確認或計時器自動觸發，後端都會啟動一個**非同步背景任務**來處理完整的 AI 辨識鏈路，與主請求完全解耦，不阻塞任何 LINE 回應。

**多模態 OCR 並行辨識架構：**

背景任務對 Buffer 中的所有圖片同時發起並行辨識請求，並透過**速率限制（Rate Limiting）信號量**控制同時在途的請求數（上限為 3 個並行），超出上限的請求自動排隊等候，有效規避 Gemini API 的 RPM 限制。每張圖片最多進行 3 次重試，採指數退避（Exponential Backoff）策略，確保網路抖動不影響辨識完整性。

**生成式 AI 語義推理與憑證分類：**

這裡是 AcctAssist 區別於傳統 OCR 的關鍵所在。Gemini 不只是做光學字元辨識，更透過**生成式 AI 的語義理解能力**對每張圖片進行多層次推理：

- **文件類型判斷**：區分正式憑證（電子發票、統一發票、收據、勞務報酬單、保險單、住宿單等）與非憑證圖片（商品實拍照）。
- **結構化資訊擷取**：依文件類型萃取對應的財務欄位，例如發票號碼、統一編號、消費金額（含稅、未稅、稅額）、廠商名稱、消費日期，以及勞務類的受款人、保險類的承保公司等類型專屬欄位。
- **信心評分機制**：對每個關鍵稽核欄位給出 0 到 1 的信心分數，低於設定閾值的欄位會被標記，整筆報帳單據進入「需人工複核」狀態，確保高風險資料不會直接流入審核佇列。

**Auto Split 兩階段智慧分單：**

當批次中包含多張不同廠商的憑證時，後端採用**兩階段掃描演算法**進行自動分單。第一階段全局掃描整個批次，識別所有正式憑證的位置；第二階段以每張憑證為起點劃定分組邊界。批次開頭尚未遇到憑證前的孤立物品照，則走獨立的向前關聯路徑（詳見第四階段）。每組生成一筆獨立的報帳單，同批次共享一個群組識別碼，供後台進行整捆管理操作。

---

## 第四階段：複雜財務情境的自動化關聯邏輯

這是整個系統最具業務深度的部分。報帳單建立完成後，系統會立即啟動**自動關聯引擎**，針對以下四種財務情境進行偵測與處理：

### 情境一：待退貨物品補傳（自動結清）

當批次中所有圖片均被 AI 判斷為非憑證（物品照）時，系統進入退貨關聯路徑。後端透過 Regex 從用戶備註中解析裸格式發票號碼（如 `AB-12345678`），以此精準比對該用戶名下狀態為「待退貨未結清」的歷史報帳記錄。命中則將物品照補入原單，狀態自動切換為「已結清」；若解析不到發票號碼，則 fallback 至同用戶最新一筆待退貨記錄。整個結清動作在背景靜默完成並推播通知。

### 情境二：換單作廢（單據串聯）

當用戶在備註中以括號格式標注被換掉的舊發票號碼時，系統以此為訊號執行**換單作廢處理**：查找對應的原始單據，將其標記為邏輯刪除並更新為作廢狀態，同時在新單上建立指向舊單的父子關聯關係，完整保留換單的追蹤鏈路。財務人員在後台可清楚看到整個換單歷程，不會有孤立的作廢記錄干擾帳務。

### 情境三：折讓單自動連結（淨額自動計算）

當 AI 語義推理識別到上傳的文件為折讓單，並從中解析出原始發票號碼時，後端自動進行發票號碼精準比對；若比對不到，則進一步採用廠商名稱前綴 + 7 天時間窗口的模糊搜尋策略進行備援比對。命中原始發票後，系統自動建立折讓關聯，並即時計算原單的實際淨付金額（原始金額加上負值折讓金額），全程無需人工核算。

### 情境四：差額補足對帳（三維模糊搜尋）

當用戶在備註中以結構化格式描述原始收據的日期與金額時，系統啟動**三維模糊搜尋演算法**：日期容許 ±3 天偏差、金額容許 ±10% 區間、廠商名稱採三段降精度策略（完整名稱 → 前 4 字元前綴比對 → 無廠商條件兜底），在這三個維度的交集中找到最可能的原始單據，建立差額補足的關聯關係，形成完整的費用支付歷程。

> **設計原則**：以上四種關聯邏輯均採用**失敗隔離設計**，任何關聯流程的例外狀況均只記錄日誌，不影響主報帳單的正常建立與後續流程。

---

## 第五階段：結果推送至 Dashboard 及異常件補件自動化循環

**正常路徑結果推播：**

所有報帳單建立完成後，後端透過 LINE Push Message API 主動推播結果通知給用戶，內容包含案件流水號（格式：`EXP-YYYYMM-NNNN`）、辨識到的金額摘要等關鍵資訊。流水號的產生機制採用當月最大序號遞增策略，並內建 5 次重試機制防範高併發下的序號碰撞。

**管理控制台 RESTful API 架構：**

後端提供完整的 RESTful API 供 Vue 3 前端管理控制台（Dashboard）對接，所有端點均以 JWT 進行身份驗證，Token 有效期 60 分鐘。主要功能端點包含：

- **清單查詢 API**：支援多維度過濾（狀態、日期區間）與分頁，預設過濾邏輯刪除的作廢記錄，確保帳務列表的乾淨性。
- **詳情查詢 API**：回傳單筆報帳的完整資訊，以及按上傳順序排列的所有憑證圖片清單與 AI 辨識詳細結果。
- **審核操作 API**：核准與退回端點獨立設計，退回端點附帶退回原因欄位。
- **重新辨識 API**：允許審核人員觸發特定單據的 AI 重新辨識，更新辨識結果。
- **CSV 匯出 API**：支援相同過濾條件的批量匯出，附帶 BOM 標頭確保 Excel 相容性。

**異常件補件自動化循環：**

這是整個流程閉環的關鍵一環。當審核人員在 Dashboard 執行退回操作時，若後端開啟了推播通知開關，系統會即時透過 LINE Flex Message 向用戶推送退件通知，訊息中嵌入快捷操作按鈕。用戶點擊後觸發一個 Postback 事件，後端解析事件中的報帳單識別碼，將用戶的對話狀態切換至「補件上傳模式」，綁定對應的原始單據 ID。

用戶在此狀態下上傳的任何新圖片，後端都會執行**單張憑證即時 OCR**——與批次 OCR 不同，補件模式採用同步辨識以確保用戶能立即得到反饋——辨識完成後直接覆蓋原始單據的圖片與所有 OCR 欄位，狀態更新為「補件待審」，重新進入審核佇列。完成後清除用戶的補件對話狀態，推播補件成功摘要。

整個補件循環形成了一個完整的**自動化閉環**：退回通知 → 用戶補件 → 系統更新 → 重回審核佇列，全程無需任何人工協調介入。

---

## 技術架構總結

| 技術層 | 核心機制 | 設計目標 |
|--------|---------|---------|
| 前端接入層 | LINE Webhook + 對話狀態機 | 零摩擦用戶體驗 |
| 非同步任務層 | 背景任務 + 60 秒 Debounce | 解耦請求與處理，批次採集 |
| AI 辨識層 | Gemini 多模態 OCR + 語義推理 + 信心評分 | 高準確率結構化資訊擷取 |
| 關聯引擎層 | 四情境自動偵測 + 三維模糊搜尋 | 複雜財務情境全自動對帳 |
| 後台管理層 | RESTful API + JWT + Vue 3 Dashboard | 高效審核 + 完整稽核軌跡 |
| 補件閉環層 | Flex Message 推播 + Postback 狀態機 | 退件補件全自動化循環 |

AcctAssist 的整體架構以「**事件驅動 + 非同步解耦 + 生成式 AI 語義理解**」為三大技術支柱，在不改變用戶習慣的前提下，將傳統人工作業流程轉化為高度自動化的智慧化系統。
